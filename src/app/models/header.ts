export interface Header {
  homepage: Homepage;
}

export interface Homepage {
  logo: string;
  title: string;
  loginButton: string;
  signUpButton: string;
  image: string;
  welcomeContent: string;
  content: string;
  footer: string;
}
